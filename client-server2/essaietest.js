/*const debug=require('debug')
const c=debug('io')
const d=c("je suis la")*/
//SELECT
//.get recuperer la premiere ligne d'un select
//.all recuperer toute les ligne d'un select
//.each recuperer les lignes une par une d'un select
//CREATE, INSERT,UPDATE
//.run



//, les messages envoyés de tous les utilisateurs, tous les évènements survenus dans les groupes, tous les groupes crées, tous les messages échangés dans les groupes, ...
/* bd.close( err=>{
    if(err){throw err}
    console.log('database closed')
});*/
// ALTER TABLE thetable ADD COLUMN category;